# Artifacts / packaging
release/
.ui-shots/
artifacts/
*.rsz
dist
dist-electron
node_modules
.DS_Store
*.log
.env
.env.*
*.local
.vscode
.idea

# Workspace-local AI state (evidence cache, audit log, feedback)
.resume-studio/
.ai-feedback.jsonl

# Product AWS deploy output (account id, stack outputs)
aws/local.json

# AI control plane build output. `dist` above only matches the repo root.
backend/dist/
backend/dist-bundle/

# Backend runtime secrets. Real values live in AWS Secrets Manager.
backend/.env
backend/.env.*
